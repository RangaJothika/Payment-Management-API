# Payment-Management-API
Implemented a Spring Boot RESTful Payment Management API using Spring MVC, Hibernate, and MySQL. Executed CRUD operations, entity relationships, and business logic for orders, payments, payment details, and reviews. Tested REST endpoints using Postman with validation and exception handling.
